name: Windows

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  BUILD_TYPE: Release

jobs:
  msvc_build:
    runs-on: windows-latest
    name: MSVC x64 Build

    steps:
    - uses: actions/checkout@v3

    - name: Create Build Environment
      run: cmake -E make_directory ${{runner.workspace}}/build

    - name: Configure MSVC Build
      shell: bash
      working-directory: ${{runner.workspace}}/build
      run: cmake -A x64 -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} $GITHUB_WORKSPACE

    - name: Build with MSVC
      working-directory: ${{runner.workspace}}/build
      run: |
        $threads = (Get-CimInstance Win32_ComputerSystem).NumberOfLogicalProcessors
        cmake --build . --config ${{env.BUILD_TYPE}} --parallel $threads

  mingw_build:
    runs-on: windows-latest
    name: MinGW Build
    strategy:
      matrix:
        sys: [mingw64, ucrt64]
    defaults:
      run:
        shell: msys2 {0}

    steps:
    - uses: actions/checkout@v3

    - uses: msys2/setup-msys2@v2
      with:
        release: false
        msystem: ${{ matrix.sys }}
        update: true
        install: mingw-w64-x86_64-toolchain mingw-w64-x86_64-cmake ninja lld
        
    - name: Set MinGW Environment Variables
      run: |
        echo 'export PATH="/usr/bin/mingw64/bin:$PATH"' >> $GITHUB_ENV
        echo 'export CC="/usr/bin/mingw64/bin/gcc"' >> $GITHUB_ENV
        echo 'export CXX="/usr/bin/mingw64/bin/g++"' >> $GITHUB_ENV

    - name: Configure MinGW Build
      run: cmake -B build -G "MSYS Makefiles" -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} $GITHUB_WORKSPACE
        
    - name: Build with MinGW
      run: cmake --build build
